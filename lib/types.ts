export type Program = {
  id: number;
  title: string;
  organization: string;
  summary: string;
  benefit: string;
  target: string;
  region: string;
  category: string;
  status: string;
  period: string;
  deadline: string;
  requirements: string[];
  documents: string[];
  how_to: string;
  official_url: string;
  notice_url: string;
  contact: string;
  source: string;
  tags: string;
};
// DEMO 데이터와 초기 화면이 공유하는 지원사업의 기본 필드 구조다.
